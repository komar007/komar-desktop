#include <xmobar.rc>

Config {
        font = "-misc-fixed-*-*-*-*-10-*-*-*-*-*-*-*",
        bgColor = "black",
        fgColor = "#555753",
        iconRoot = ICON_ROOT,
        iconOffset = ICON_OFFSET,
        position = Static { xpos = POS, ypos = YPOS, width = WIDTH, height = HEIGHT },
        lowerOnStart = False,
        commands = [ Run Com  "/home/komar/.xmonad/morning.sh" [] "morning" 100,
                     Run Weather "EPKT" [
                        "-t", "<tempC>°C",
                        "-L","10","-H","30",
                        "--normal","#b8bb26", "--high", "#fb4934", "--low","#83a5d8"
                     ] 3000,
                     Run Date "%a %b %_d %k:%M:%S" "date" 10
        ],
        sepChar = "%",
        alignSep = "}{",
#ifdef SIDE_LEFT
        template = "}{ <fc=#d79921>%date%</fc>"
#else
        template = "}{ %morning% %EPKT% <fc=#d79921>%date%</fc>"
#endif
}

-- vim:ft=haskell
